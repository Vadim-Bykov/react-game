export const getGameTime = (state) => state.stats.gameTime;
export const getMinutes = (state) => state.stats.minutes;
export const getSeconds = (state) => state.stats.seconds;
export const getTrueAttempts = (state) => state.stats.trueAttempts;
export const getFalseAttempts = (state) => state.stats.falseAttempts;
// export const getCountGames = (state) => state.stats.countGames;
